import { SharedFormDirective } from './shared-form.directive';

describe('SharedFormDirective', () => {
  it('should create an instance', () => {
    const directive = new SharedFormDirective();
    expect(directive).toBeTruthy();
  });
});
